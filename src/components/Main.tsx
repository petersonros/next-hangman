import styles from '../styles/Home.module.css';

const Main: React.FC = () => (
  <main className={styles.main}>
    <h1 className={styles.title}>Hangman Game</h1>
    {/* Adicione aqui o conteúdo do seu index.html */}
  </main>
);

export default Main;
